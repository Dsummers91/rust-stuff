use card::{Card, Suit};

pub struct Hand {
  pub cards: Vec<u8>
}


impl Hand {
  fn new(self: &Hand) {
    
  }
}

#[cfg(test)]
  mod tests {
  use super::*;
    #[test]
    fn should_be_true() {
    assert_eq!(2 + 2, 4);
    }
}

